var money = 12;
var friends = 1;
var isWeekend = false;

// If it is the weekend and friends is greater than 1 or money is greater than 15.
if ((isWeekend && friends > 1) || (money > 15)) {
  console.log('It is the weekend!')
}

var lightIsTurnedOn = true;

// ! - not
if (!lightIsTurnedOn) {
  
}
